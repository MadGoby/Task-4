type RefreshIntervalPositions = {
  target: string;
  position: string;
  sliderWidth: number;
  handelWidth: number;
};

type IntervalDisplayData = {
  isDouble: boolean,
  handleWidth: number
};

export {
  RefreshIntervalPositions,
  IntervalDisplayData,
};
