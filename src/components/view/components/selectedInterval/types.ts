type RefreshIntervalPositions = {
  target: string;
  position: string;
  sliderWidth: number;
  handleWidth: number;
};

type IntervalDisplayData = {
  isDouble: boolean,
  handleWidth: number
};

export {
  RefreshIntervalPositions,
  IntervalDisplayData,
};
